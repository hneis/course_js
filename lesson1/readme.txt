4. 1000 + "108", так как одно из слогаемых строка, все остальные приводятся к строковому типу,
т.е. произойдет конкатенация строк, результатом которой будет "1000108"
5.  defer - загружается когда DOM дерево готово, но до события DOMContentLoaded, сохроняют порядок относительно друг друга
    async - обрабатывается асинхронно, не ждет события DOMContentLoaded
    оба аттрибута не блокируют загрузку страницы
